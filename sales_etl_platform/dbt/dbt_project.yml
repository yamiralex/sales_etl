name: sales_etl
version: '1.0'
config-version: 2

profile: sales_etl

model-paths:
  - models
test-paths:
  - tests
macro-paths:
  - macros

models:

  sales_etl:

    silver:
      +materialized: table
      +schema: SILVER

    gold:
      +materialized: table
      +schema: GOLD